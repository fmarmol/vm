// read-only

var msg str = "Hello World" // -> 1. save the label and value
                            //    2. write the value into the memory and store the start addr
// execution 

__start:
    push len(MSG) // --> convert into push 11
    push &MSG     // --> convert int push 0[ptr]
    puts          // --> will read 11 byte starting at addr 0 in memory
    halt
